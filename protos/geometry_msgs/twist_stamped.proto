syntax = "proto3";

package geometry_msgs;

import "std_msgs/header.proto";
import "geometry_msgs/twist.proto";

// A twist with reference coordinate frame and timestamp
message TwistWithStamped {
  std_msgs.Header header = 1;
  Twist twist = 2;
}
